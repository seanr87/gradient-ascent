# LOCKED — what no automated change may alter

`tasks/climb-tue-process-review.md` reads this file before it changes anything. An automated change (a routine editing `tasks/`, `SCHEDULED-TASKS.md`, or a routine's schedule or state) may not alter anything listed here. A change that would is not made, and the review says so in the ledger. Only Sean edits this file.

1. **Sleeper is never touched and no credentials are ever added.** Decisions only; Sean executes. (`tasks/COMMON.md`, "Who you are")
2. **Past decisions, posts and notes are never edited or deleted.** Corrections go in a new file or the column. (`tasks/COMMON.md`, "Writing the decision")
3. **The site voice.** First-person Claude, dry, confident, lightly sarcastic, no exclamation points; Sean is the clipboard. (`tasks/COMMON.md`, "Writing the decision")
4. **The decision-file structure and the 400-word cap.** Deadline line, clipboard first, `## Why`, optional `## Detail`; 400 words excluding the clipboard, column exempt at 500–900. (`tasks/COMMON.md`, "Writing the decision")
5. **The three-line run report.** Deadline line, clipboard block, page link, nothing else. (`tasks/COMMON.md`, "Reporting")
6. **The ledger rules and the 150-line cap.** (`tasks/COMMON.md`, "The ledger")
7. **The two-change limit.** The process review makes at most two changes a week, each one commit. (`tasks/climb-tue-process-review.md`)
8. **The revert rule.** Every change carries a measurable kill condition logged in the ledger, and the next review reverts a change whose kill condition was met. (`tasks/climb-tue-process-review.md`)
9. **This file.**

Everything else in `tasks/`, `SCHEDULED-TASKS.md` and the agent-owned routines is the process review's to change, within the limits above. Nothing is deleted; git history and disabling are the archive.
